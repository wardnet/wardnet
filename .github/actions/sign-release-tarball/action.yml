name: Sign release tarball
description: >-
  sha256 + minisign-sign a release tarball and verify the signature against the
  committed Wardnet public key. Produces <tarball>.sha256 and <tarball>.minisig
  beside the input. The encrypted private key is written to a temp file and
  shredded on exit. Secrets are passed as inputs (composite actions cannot read
  `secrets` directly) and are masked by the caller's `${{ secrets.* }}` expansion.

inputs:
  tarball:
    description: Path to the .tar.gz to sign (workspace-relative).
    required: true
  comment:
    description: minisign trusted comment (-t), e.g. "wardnet-bridge 0.1.0 (aarch64)".
    required: true
  pubkey:
    description: Path to the minisign public key used to verify.
    required: false
    default: deploy/keys/wardnet-release.pub
  minisign-key:
    description: >-
      Encrypted minisign private key (secrets.WARDNET_MINISIGN_KEY), either as
      the raw key file or base64-encoded.
    required: true
  minisign-password:
    description: Password for the minisign private key (secrets.WARDNET_MINISIGN_PASSWORD).
    required: true

runs:
  using: composite
  steps:
    - name: Install minisign
      shell: bash
      run: sudo apt-get update && sudo apt-get install -y minisign

    - name: sha256 + sign + verify
      shell: bash
      env:
        TARBALL: ${{ inputs.tarball }}
        COMMENT: ${{ inputs.comment }}
        PUBKEY: ${{ inputs.pubkey }}
        MINISIGN_KEY: ${{ inputs.minisign-key }}
        MINISIGN_PASSWORD: ${{ inputs.minisign-password }}
      run: |
        set -euo pipefail

        if [[ -z "${MINISIGN_KEY:-}" || -z "${MINISIGN_PASSWORD:-}" ]]; then
          echo "::error::WARDNET_MINISIGN_KEY and WARDNET_MINISIGN_PASSWORD are required. See deploy/keys/README.md."
          exit 1
        fi

        # The secret is shared with other repos, which consume the key file
        # verbatim, so it is stored raw. Historically it was base64-encoded.
        # Accept either: a raw minisign key always opens with the "untrusted
        # comment:" header, anything else is treated as base64.
        KEYFILE="$(mktemp)"
        if [[ "${MINISIGN_KEY}" == untrusted\ comment:* ]]; then
          printf '%s\n' "${MINISIGN_KEY}" > "${KEYFILE}"
        else
          printf '%s' "${MINISIGN_KEY}" | base64 -d > "${KEYFILE}"
        fi
        chmod 0600 "${KEYFILE}"
        trap 'shred -u "${KEYFILE}" || true' EXIT

        if ! head -n1 "${KEYFILE}" | grep -q '^untrusted comment:'; then
          echo "::error::WARDNET_MINISIGN_KEY is neither a raw minisign key nor valid base64 of one. See deploy/keys/README.md."
          exit 1
        fi

        sha256sum "${TARBALL}" | awk '{print $1}' > "${TARBALL}.sha256"

        # Password via both stdin and env — tty-less runners need stdin on older
        # minisign, MINISIGN_PASSWORD on newer.
        printf '%s\n' "${MINISIGN_PASSWORD}" | \
          MINISIGN_PASSWORD="${MINISIGN_PASSWORD}" minisign -Sm "${TARBALL}" \
            -s "${KEYFILE}" \
            -t "${COMMENT}" \
            -c "Wardnet release signing key" \
            -x "${TARBALL}.minisig"

        minisign -Vm "${TARBALL}" -p "${PUBKEY}" -x "${TARBALL}.minisig"
