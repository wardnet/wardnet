# cargo-audit configuration
# https://github.com/rustsec/rustsec/tree/main/cargo-audit

[advisories]
# RUSTSEC-2024-0436: paste crate unmaintained
# Compile-time proc-macro pulled in via netlink-packet-core (used by
# rtnetlink for kernel policy routing). Not a CVE — the author stopped
# publishing updates. Zero runtime footprint. Re-evaluate if the
# netlink-rs ecosystem migrates away from paste, or consider filing a
# PR upstream at rust-netlink/netlink-packet-core.
#
# RUSTSEC-2026-0097 / GHSA-cq8v-f236-94qc: rand unsound with a custom
# logger using `rand::rng()`.
# Transitive via sqlx-postgres and a few other crates. Fixed in rand
# 0.9.3, but our transitives still pin older 0.8 / 0.9.2 lines. We
# don't install a custom logger that touches rand internals, so the
# unsoundness is unreachable in our usage. Re-evaluate when sqlx
# bumps rand.
#
# RUSTSEC-2026-0118: hickory-proto NSEC3 closest-encloser proof
# validation enters an unbounded loop on cross-zone responses.
# Transitive via dhcproto 0.14.0, which pins hickory-proto ^0.25.2
# with `default-features = false`. The vulnerability is in the
# DNSSEC validation code, which isn't compiled without the dnssec
# features. No 0.25.x backport exists; the fix is only in the
# 0.26.x line, and dhcproto hasn't bumped yet. Re-evaluate when
# dhcproto upgrades to hickory-proto 0.26.
#
# RUSTSEC-2026-0119: hickory-proto O(n²) name compression during
# message encoding. Transitive via dhcproto 0.14.0 (same pin as
# above). Triggered by DNS messages with many compressible names;
# wardnetd only uses dhcproto to encode DHCPv4 packets, which carry
# at most a single FQDN (option 81), so the quadratic blow-up isn't
# reachable. No 0.25.x backport. Re-evaluate when dhcproto upgrades.
#
# RUSTSEC-2026-0186: memmap2 unchecked pointer offset.
# Transitive via pyroscope (continuous-profiling client) and
# symbolic-common (its demangler). The unsoundness is in memmap2's
# offset API; pyroscope only memory-maps its own profiling buffers with
# in-bounds offsets, so the unchecked path isn't reachable in our usage.
# No fixed release yet. Re-evaluate when pyroscope bumps memmap2.
ignore = [
    "RUSTSEC-2024-0436",
    "RUSTSEC-2026-0097",
    "RUSTSEC-2026-0118",
    "RUSTSEC-2026-0119",
    "RUSTSEC-2026-0186",
]
