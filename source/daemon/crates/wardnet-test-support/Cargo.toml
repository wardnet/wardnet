[package]
name = "wardnet-test-support"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# https://crates.io/crates/tracing
tracing.workspace = true
# https://crates.io/crates/tracing-subscriber
tracing-subscriber.workspace = true
# https://crates.io/crates/uuid
# Needed by the `principal` helpers, which mint test `AuthContext`s from ids.
uuid.workspace = true
# Workspace crate: owns `AuthenticatedUser`, whose constructor these helpers
# wrap so test stubs across the workspace don't each call it directly.
wardnet-common.workspace = true
